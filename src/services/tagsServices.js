

// import { Tags } from "../schemasModle/model.js";

// export const createTagsService = async ({ body }) => Tags.create(body);

// export const readAllTagsService = async ({
//   find = {},
//   sort = "",
//   limit = "",
//   skip = "",
//   select = "",
// }) => Tags.find(find).sort(sort).limit(limit).skip(skip).select(select);

// export const readSpecificTagsService = async ({ id }) => Tags.findById(id);

// export const deleteSpecificTagsService = async ({ id }) =>
//   Tags.findByIdAndDelete(id);

// export const updateSpecificTagsService = async ({ id, body }) =>
//   Tags.findByIdAndUpdate(id, body, {
//     new: true,
//     runValidators: true,
//   });
